package hat;

import hat.results;

public def assertTrue(value: Boolean) => {
    return if value {
        return results.success();
    } else {
        return results.failure("Expected true, got false");
    };
}
