module hat.tests.pass;

members {
    testCases
}

import hat.TestCase;
import hat.assertTrue;

val testCases = listOf[TestCase](
    TestCase("can assert that things are true", fun() =>
        assertTrue(true)
    )
);
