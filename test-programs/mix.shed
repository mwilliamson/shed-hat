package hat.tests;

import hat.TestCase;
import hat.assertTrue;

public val mix = listOf[TestCase](
    TestCase("Should pass", fun() =>
        assertTrue(true)
    ),
    TestCase("Should fail", fun() =>
        assertTrue(false)
    ),
    TestCase("Should also fail", fun() =>
        assertTrue(false)
    ),
    TestCase("Should also pass", fun() =>
        assertTrue(true)
    )
);
