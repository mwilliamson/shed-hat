package hat.tests;

import hat;

public val mix = listOf(
    hat.TestCase("Should pass", fun() =>
        hat.assertTrue(true)
    ),
    hat.TestCase("Should fail", fun() =>
        hat.assertTrue(false)
    ),
    hat.TestCase("Should also fail", fun() =>
        hat.assertTrue(false)
    ),
    hat.TestCase("Should also pass", fun() =>
        hat.assertTrue(true)
    )
);
